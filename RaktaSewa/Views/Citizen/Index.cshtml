@*using RaktaSewa.Models.ViewModels.CitizenViewModel*@
@*@model RaktaSewa.Models.ViewModels.CitizenViewModel*@
@model IEnumerable<RaktaSewa.Model.Citizen>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
@*<form asp-controller="Citizen" asp-action="Index" method="get">
    <p>
        <select asp-for="MovieGenre" asp-items="Model.Genres">
            <option value="">All</option>
        </select>

        Title: <input type="text" asp-for="SearchString" />
        <input type="submit" value="Filter" />
    </p>
</form>*@
<table class="table">
    <thead>
        <tr>
           <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.blood_group)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.blood_group)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.citizen_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.citizen_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.citizen_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>